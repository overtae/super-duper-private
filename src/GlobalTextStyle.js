import { css } from "styled-components";

const SemiBoldText = css`
  font-weight: 700;
  font-size: 16px;
  line-height: 21px;
  letter-spacing: -1%;
  text-align: center;
`;

const BoldText = css`
  font-weight: 700;
  font-size: 20px;
  line-height: 26px;
  letter-spacing: -1%;
`;

const MediumText = css`
  font-weight: 400;
  font-size: 16px;
  line-height: 21px;
  letter-spacing: -1%;
`;

export { SemiBoldText, BoldText, MediumText };
